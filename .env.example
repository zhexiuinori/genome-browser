# API Base URL (optional - defaults to proxy configuration)
# If your backend is running separately, set this to the backend URL
# Example: http://localhost:4000 or https://api.gpgdb.example.com
REACT_APP_API_BASE_URL=

# Leave empty to use the proxy configuration in package.json
